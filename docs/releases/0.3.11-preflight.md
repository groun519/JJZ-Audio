# JJZero Audio 0.3.11 사전 마감표

> 상태: **BLOCKED**. 로컬 후보 검증은 통과했지만 Authenticode 코드 서명 인증서가 없어 공개 릴리스 게이트를 통과하지 못했습니다.

## 범위

- 기준 공개 버전: `0.3.10` (`v0.3.10`)
- 목표 버전: `0.3.11`
- 검증 빌드 소스: `dd51b8bf188fbbaeb73fbcccb74f0ef74a50363a`
- 애플리케이션: `0.3.11`
- AI Runtime: `5`
- GPU 프로필: CUDA 12.8 `1`, DirectML `2`, Windows ROCm `2`
- 공개 태그 및 GitHub Release: 생성하지 않음

## 자동 검증

- 전체 Python 테스트 `1,653`개 통과 (`150.648s`)
- Google Drive, OAuth, 다운로드, 공유, 모델 패키지 전용 테스트 `62`개 통과
- `src`, `tests`, `scripts` Python 컴파일 검증 통과
- PowerShell 릴리스 스크립트 구문 검사 통과
- `git diff --check` 통과
- 배포 폴더와 구성요소 매니페스트 검증 통과
- 외부 빌드 호스트의 UCRT/API-MS DLL이 설치본에 섞이지 않는지 검증

## 앱과 런타임

- Windows 10에서 패키지 앱 startup smoke 통과: `3,255.5ms`
- AI Runtime 5 전체 패키지 압축 해제, 시스템 진단, 패키지 앱 재기동 통과
- Runtime 5 압축 해제 크기: `7,343,475,169` bytes
- Runtime 5의 part01-part04는 공개 `v0.3.0` Runtime 4 자산과 크기, 압축 해제 크기, 파일 수, SHA-256이 모두 같아 불변 URL을 재사용
- 새 자산은 `JJZero-Runtime-5-part05.zip` 1개: `69,178,439` bytes, SHA-256 `08cfdd047bf71967bcd4034330378ec2ff0e84b40743714d800d5528e9b343e2`
- 재사용하는 공개 Runtime/GPU 자산 15개의 GitHub 크기와 digest 일치 확인
- RTX 3060 CUDA 환경에서 2초 합성 WAV를 Runtime 5 Demucs로 실제 분리하고 `vocals.wav`, `no_vocals.wav` 2초 출력을 확인

## 설치 수명주기

- 공개 `0.3.10` 설치본에서 `0.3.11` 후보로 실제 업데이트 통과
- 관리 저장소 이동 후 라이브러리, 모델, 설정, 출력, Runtime 사용자 파일 보존 확인
- Runtime 5 실제 설치 후 배포 검증 통과
- Normal Uninstall `Managed`, `External` 2개 시나리오 통과
- Complete Removal `PreserveWork`, `DeleteWork`, `MissingGenerated`, `ProtectedAppState`, `OverlappingRoots`, `BrokenSettings` 6개 시나리오 통과
- 사용자/시스템 환경 변수, Python 명령과 레지스트리, GPU 드라이버, 서비스, 시작 항목, JJZero 파일 연결의 설치 전후 차이 0건
- 검증 증거: `build/verification/0.3.11-system-footprint.json`

## Google Drive

- 로컬 Credential Manager의 정확한 `JJZero Audio/Google Drive` Target 제거와 다른 자격 증명 보호를 검증
- OAuth, 업로드, 다운로드, 공유, 롤백, 모델·학습 패키지 흐름은 격리된 자동 테스트로 검증
- 실제 원격 Google Drive 연결과 공유는 전용 테스트 계정 자격 증명이 없어 실행하지 않음

## 설치 프로그램

- 파일: `JJZero-Audio-0.3.11-Setup.exe`
- 제품 버전: `0.3.11`
- 크기: `92,020,137` bytes (`87.76 MiB`)
- SHA-256: `ee452056188fa9f487cc47b3c007926681112976968a0da007905a0ee48c8af7`
- 매니페스트: `latest.json`, `7,072` bytes
- 매니페스트 SHA-256: `a45b4e733ac525b62e0ef9e3ff16b095da80e833c1872cc986a4842a210b5ee3`
- 로컬 매니페스트 스냅샷: `release/v0.3.11-latest.json`

## 차단 사유

- `Cert:\CurrentUser\My`와 `Cert:\LocalMachine\My`에 개인 키가 있는 Code Signing 인증서가 없음
- JJZero 코드 서명 환경 변수도 구성되지 않음
- 설치 프로그램 상태: `NotSigned`
- Release Readiness 결과: `Public releases require Authenticode metadata. Rebuild with -RequireCodeSigning.`

## 판정

- 소스, 앱 빌드, Runtime 5, 실제 CUDA 분리, 설치, 업데이트, Normal/Complete 제거와 시스템 비침투 검증은 통과했습니다.
- 유효한 Authenticode 인증서로 앱과 설치 프로그램을 다시 서명하고 동일 게이트를 재실행하기 전에는 `v0.3.11` 태그와 GitHub Release를 만들지 않습니다.
- 전용 Google Drive 테스트 계정이 제공되면 원격 연결·공유 E2E도 추가 실행합니다.
